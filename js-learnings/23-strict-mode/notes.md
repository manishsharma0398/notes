# Chapter 23 Notes
**Enable:** `"use strict";` at top  
**Always strict:** Classes, modules

## Changes
- No implicit globals
- this = undefined in functions
- No with, octals
- eval own scope
- delete restrictions

## One-Sentence
Strict mode is an opt-in JavaScript mode that eliminates silent errors by throwing them instead, prevents unsafe actions like implicit globals and with statements, and is automatically enabled in classes and modules.
