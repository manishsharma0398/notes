live/
├── root.hcl                    ← root parent (generate blocks, remote_state)
├── .gitignore                  ← must include .terragrunt-cache/
│
├── non-prod/
│   ├── env.hcl                 ← env-level locals: account_id, role_arn, region
│   ├── staging/
│   │   ├── env.hcl             ← stage-level overrides (optional)
│   │   ├── vpc/
│   │   │   └── terragrunt.hcl
│   │   ├── ecs-cluster/
│   │   │   └── terragrunt.hcl
│   │   └── app/
│   │       └── terragrunt.hcl
│   └── dev/
│       ├── vpc/
│       │   └── terragrunt.hcl
│       └── app/
│           └── terragrunt.hcl
│
└── prod/
    ├── env.hcl
    ├── vpc/
    │   └── terragrunt.hcl
    ├── ecs-cluster/
    │   └── terragrunt.hcl
    ├── app/
    │   └── terragrunt.hcl
    └── worker/
        └── terragrunt.hcl

S3 state keys generated by path_relative_to_include() from live/root.hcl:
  non-prod/staging/vpc/terraform.tfstate
  non-prod/staging/ecs-cluster/terraform.tfstate
  non-prod/staging/app/terraform.tfstate
  prod/vpc/terraform.tfstate
  prod/ecs-cluster/terraform.tfstate
  prod/app/terraform.tfstate
  prod/worker/terraform.tfstate

# NOTE: Each unit's directory path IS its state key — zero manual key management.
