# Chapter 00 Examples

```
examples/
├── 01-version-pinning.tf        → required_version + required_providers, lock file explanation
├── 02-registry-anatomy.tf       → reading Required vs Optional, argument vs attribute reference
└── 03-plan-output-reading.md    → annotated real plan output covering all diff symbols
```
